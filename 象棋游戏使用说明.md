# 中国象棋人机大战游戏

## 项目简介

这是一个基于 React + Express 构建的中国象棋游戏，使用 Pikafish 引擎提供强大的 AI 支持。

## 功能特点

1. **美观的棋盘界面** - 传统风格的中国象棋棋盘
2. **双人对战模式** - 支持两人在一台设备上对战
3. **人机对战模式** - 与强大的 Pikafish AI 对战
4. **AI 建议功能** - 随时可以请求 AI 给出下一步建议
5. **悔棋功能** - 支持悔棋操作
6. **摆棋模式** - 自定义棋局开始位置

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式运行

同时运行前端和后端：

```bash
npm run dev
```

这将启动：
- 前端开发服务器 (http://localhost:5173)
- 后端 API 服务器 (http://localhost:3001)

### 仅运行前端

```bash
npm run client:dev
```

### 仅运行后端

```bash
npm run server:dev
```

### 生产构建

```bash
npm run build
```

## 项目结构

```
├── src/                    # 前端源代码
│   ├── components/        # React 组件
│   │   ├── ChessBoard.tsx # 棋盘组件
│   │   └── GameControls.tsx # 游戏控制面板
│   ├── pages/             # 页面
│   │   └── Home.tsx       # 主页面
│   ├── store/             # 状态管理
│   │   └── gameStore.ts   # 游戏状态
│   ├── types/             # TypeScript 类型
│   │   └── chess.ts       # 象棋相关类型
│   └── utils/             # 工具函数
│       └── chessLogic.ts  # 象棋规则逻辑
├── api/                   # 后端源代码
│   ├── routes/            # API 路由
│   │   └── engine.ts      # 引擎路由
│   ├── utils/             # 工具函数
│   │   └── engineManager.ts # Pikafish 引擎管理
│   ├── app.ts             # Express 应用
│   └── server.ts          # 服务器入口
└── pikafish-engine/       # Pikafish 引擎源代码
```

## Pikafish 引擎编译说明

为了使用 AI 功能，需要先编译 Pikafish 引擎：

### 1. 进入引擎目录

```bash
cd pikafish-engine
```

### 2. 编译引擎

在 Linux/Mac 上：

```bash
cd src
make -j profile-build ARCH=x86-64
```

在 Windows 上（需要 MinGW）：

```bash
cd src
mingw32-make -j profile-build ARCH=x86-64
```

### 3. 验证编译成功

编译完成后，会生成 `pikafish` (或 `pikafish.exe`) 可执行文件。

## 游戏说明

### 基本操作

1. **选择棋子** - 点击你想要移动的棋子
2. **移动棋子** - 点击绿色圆点标记的有效位置
3. **悔棋** - 点击"悔棋"按钮撤销上一步
4. **重置** - 点击"重置"按钮开始新游戏

### 游戏模式

1. **双人对战** - 两人轮流下棋
2. **人机对战** - 与 AI 对战，可选择 AI 执红或黑
3. **摆棋模式** - 自定义棋盘布局

### AI 功能

1. **AI 建议** - 点击"AI 建议"按钮，AI 会给出当前局面的最佳走法建议
2. **AI 走棋** - 在人机对战模式下，点击"让 AI 走一步"让 AI 自动走棋

## 技术栈

- **前端** - React 18 + TypeScript + Vite + Tailwind CSS
- **状态管理** - Zustand
- **后端** - Express + TypeScript
- **AI 引擎** - Pikafish (中国象棋 UCI 引擎)
- **象棋规则** - 自研实现

## 注意事项

1. 首次使用需要编译 Pikafish 引擎
2. 确保有足够的权限来执行编译后的引擎程序
3. AI 思考时间根据硬件性能有所不同
4. 如果 AI 功能不可用，可以先使用双人对战模式

## 许可证

本项目基于 GPL v3 许可证发布（继承自 Pikafish 和 Stockfish）。
